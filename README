In order to foster interoperability between robotic frameworks, while allowing the standard
practice of using configurable log levels, the ROS project defined a minimal interface for
logging called console_bridge

This package implements this interface, using Rock's logging facility.
